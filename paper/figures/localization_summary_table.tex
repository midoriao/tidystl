% Summary of tool-vs-tool divergence-localization cases (no native baseline).
% Preamble requirements: \usepackage{booktabs}
\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{5pt}
\begin{tabular}{@{}lllcl@{}}
\toprule
Case & tools compared & root robustness & effect & origin \\
\midrule
\texttt{until\_boundary}  & Breach vs.\ RTAMT  & $-5.0$ / $+2.0$ & verdict flip        & $U_{[1,2]}$ \\
\texttt{window\_sampling} & Breach vs.\ TaLiRo & $+2.0$ / $+5.0$ & value gap (no flip) & $G_{[1,2]}$ \\
\bottomrule
\end{tabular}

\medskip

\begin{tabular}{@{}lcccc@{}}
\toprule
\texttt{until\_boundary} verdict & Breach & RTAMT & TaLiRo & STLCG++ \\
\midrule
$\rho$ at $t=0$ & $-5.0$ & $\mathbf{+2.0}$ & $-5.0$ & $-5.0$ \\
\bottomrule
\end{tabular}
\caption{Mechanical divergence localization between pairs of real-tool semantics
  (no \textsc{native} reference). Top: on a bounded \texttt{until}
  (\texttt{(x>=0) U[1,2] (y>=0)}) Breach and RTAMT flip the verdict, and on a
  between-samples window Breach and TaLiRo show a value-only gap; in both the
  localizer reports exactly the listed operator and names the responsible
  primitive ($\mathtt{BoundedUntilKernel}$ vs.\ $\mathtt{DiscreteBoundedUntil}$;
  $\mathtt{WindowMin}$ vs.\ $\mathtt{SampleMin}$). Bottom: across all four tools,
  RTAMT is the lone outlier on the bounded-\texttt{until} verdict --- its
  discrete-time until composition disagrees with the three dense/sample-time
  tools --- and the divergence localizes to the single $U_{[1,2]}$ operator in
  every pairwise comparison. The cause is in the temporal semantics, not the
  syntax.}
\label{tab:e2-localization}
\end{table}

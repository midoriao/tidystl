% AUTO-GENERATED from extra/outputs/e2_localization records by
% extra/experiments/e2_localization/aggregate.py --emit-tex; do not hand-edit.
% tabular only -- wrap in your own table float / caption.
% Preamble: \usepackage{booktabs}.
% 'l/r' columns follow the 'tools compared' order (left tool / right tool).
%
% formulas (tidystl syntax):
%   until_boundary: (x >= 0) U[1,2] (y >= 0)
%   window_sampling: G[0,3]( (mode >= 0) and F[0,1]( (enable >= 0) and G[1,2](level >= 0) ) ) and (safety_margin >= 0)
%
% signals:
%   until_boundary: t=[0, 1]; x=[5, -5]; y=[-1, 2]
%   window_sampling: t=[0, 3, 4, 5, 6, 7, 8, 9]; mode=10 (const); enable=10 (const); safety_margin=9 (const); level=[3, 0, 5, 5, 5, 5, 5, 5]
\begin{tabular}{@{}llccl@{}}
\toprule
Case & Tools compared & $|\varphi|$/depth & $\rho$ & Divergence \\
\midrule
\texttt{until\_boundary} & Breach vs.\ RTAMT & 3 / 1 & $-5$ / $+2$ & verdict flip on $\Until_{[1,2]}$ \\
\texttt{window\_sampling} & Breach vs.\ TaLiRo & 10 / 3 & $+2$ / $+5$ & value gap on $\Always_{[1,2]}$ \\
\bottomrule
\end{tabular}

% kernels at origin -- until_boundary: $\Until_{[1,2]}$ Breach BoundedUntilKernel[1,2] vs.\ RTAMT DiscreteBoundedUntil[1,2]
% kernels at origin -- window_sampling: $\Always_{[1,2]}$ Breach WindowMin[1,2] overridden by _sliding_min_breach vs.\ TaLiRo SampleMin[1,2]
% div_until_boundary verdict at t=0 across tools: Breach -5, RTAMT +2 (outlier), TaLiRo -5, STLCG++ -5.
% first-divergence step: div_until_boundary t-index 0; div_window_sampling t-index 0.
% breach's window kernel here is the sliding-min variant (_sliding_min_breach).

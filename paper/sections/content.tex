% ================================================================
\section{Introduction}
\label{sec:intro}
% ----------------------------------------------------------------
% ================================================================

Signal Temporal Logic (STL) is a standard formalism for specifying temporal
properties of real-valued signals~\cite{maler2004monitoring,fainekos2009robustness}.
Its quantitative \emph{robust} semantics measures how strongly a behavior
satisfies or violates a specification, and the resulting margin underpins runtime
monitoring~\cite{bartocci2018survey}, falsification~\cite{annpureddy2011staliro},
and learning-enabled design~\cite{raman2014mpc}.

The robust semantics is defined over an idealized continuous signal, yet tools
usually evaluate it on finite, discretely sampled traces.
This requires evaluator-level choices that are not determined by the STL formula and samples alone, such as interpolation and end-of-trace behavior.
We call these choices \emph{implicit semantics}; \cref{tab:divergence} organizes the main sources of divergence.

Because these choices are often embedded inside each tool, two STL tools
can report different robustness values or Boolean satisfaction verdicts for
the same formula and trace.
Such discrepancies have appeared in practice: in the ARCH-COMP 2021
falsification-tool competition~\cite{ernst2021archcomp}, post-hoc validation
identified differences related to tool-specific evaluation choices.
These cases show that cross-tool discrepancies are not only a matter of
agreement, but also of diagnosis.

To make such discrepancies actionable, we compare evaluator behaviors not only
at the final output but also along the formula structure.
This turns cross-tool discrepancies into diagnostic targets: the question is not
only whether two tools agree, but where their evaluations first diverge.

We propose \tool,%
\footnote{Artifact repository: \url{https://github.com/midoriao/tidystl}.}
a diagnostic toolkit for localizing such discrepancies.
It evaluates the same formula and trace under named tool behaviors
while aligning intermediate robustness values along a shared formula structure
(\cref{fig:arch}).
This allows \tool to localize where two evaluator behaviors first diverge.

This paper makes the following contributions.
\begin{itemize}
  \item We organize the implicit semantic choices in STL robustness
    evaluation and show that they can affect robustness values and Boolean satisfaction verdicts (\cref{sec:background,tab:divergence-profile}).
  \item We propose \tool, with a common interface and representation for STL evaluator behavior, allowing both existing tools and new evaluator variants to share a single formula-and-trace front end (\cref{sec:design,fig:arch}).
  \item We turn cross-tool discrepancies into diagnosable evaluation differences by localizing
  where two evaluator behaviors first diverge.
  We demonstrate this with compatibility backends for Breach~\cite{donze2010breach}, TaLiRo~\cite{annpureddy2011staliro}, and RTAMT~\cite{nickovic2020rtamt}
    (\cref{sec:discrepancies,tab:e2-localization}).
\end{itemize}

\paragraph{Related Work.}
In this paper, we focus on the comparison of \tool{} with Breach, RTAMT, and TaLiRo, which employ the quantitative STL semantics of Donz\'e and Maler~\cite{donze2010robust} or Fainekos and Pappas~\cite{fainekos2009robustness} as their underlying theory.
\cref{section:detailed_comparison} lists some other related tools.
Extending \tool{} to support other quantitative STL semantics, such as the RoSI semantics~\cite{deshmukh2017online}, is an interesting future direction.

% ================================================================
\section{Implicit Semantic Choices in STL Evaluation}
\label{sec:background}
% ----------------------------------------------------------------
% ================================================================

\begin{table}[t]
\centering
\caption{Representative \emph{implicit choices} in robustness evaluation~\cite{donze2010robust}.
These choices can affect both reported
robustness values and the accepted class of formula-trace inputs.
Appendix~\ref{sec:formal} further explores their scope.}
\label{tab:divergence}

\input{figures/choice-table.tex}
\end{table}


We take the quantitative STL semantics of Donz\'e and
Maler~\cite{donze2010robust} as the representative theoretical reference.
Together with the closely related framework by Fainekos and Pappas~\cite{fainekos2009robustness},
it represents the practical setting used by most of the existing STL monitoring and falsification tools~\cite{ernst2021archcomp};
our focus is on evaluator-level variations within this setting.

To fix the scope, we recall only the definitions needed to delimit where
implicit semantic choices arise in evaluation.
For completeness, Appendix~\ref{sec:formal}
records the standard full inductive definition and further scope distinctions.

\begin{definition}[Signal]
\label{def:signal}
A \emph{signal} over a set of variables $\mathbf{Var}$ is a function
$\sigma \colon \mathbb{R}_{\geq 0} \to \mathbb{R}^{\mathbf{Var}}$.
\end{definition}

\begin{definition}[STL syntax]
\label{def:stl}
STL formulas $\varphi \in \mathbf{Fml}$ are given by:
\[
  \small
  \mathbf{Fml} \ni \varphi ::= {}
    \top \mid f(\vec{x}) \geq 0
    \mid \neg\varphi \mid \varphi_1 \vee \varphi_2
    \mid \varphi_1 \wedge \varphi_2
  \mid \varphi_1 \mathbin{\mathcal{U}_I} \varphi_2
    \mid \varphi_1 \mathbin{\mathcal{R}_I} \varphi_2
    \mid \Diamond_I\varphi \mid \Box_I\varphi .
\]
Here $f$ is a function symbol, for example $f(x, y) = 4x - y - 3$, and $I$ is a non-singular interval in $\mathbb{R}_{\geq 0}$, for example $[2,5]$, $(1,3]$, or $[0,\infty)$.
\end{definition}

\noindent
Informally, the robust semantics $\rho(\varphi, \sigma, t)$ replaces the Boolean
verdict with a signed real-value,
e.g., $\rho(x - 3 \geq 0, \sigma, t) = \sigma_x(t) - 3$.
The Boolean connectives and temporal operators then aggregate the robustness values using $\min$, $\max$, $\inf$, and $\sup$; for example,
$\rho(\Box_{[0,2]}\,(x - 3 \geq 0), \sigma, t)
  = \inf_{t' \in [t,\,t+2]} (\sigma_x(t') - 3)$.

We call a design choice in a concrete evaluator's implementation an \emph{implicit semantic choice} if
the choice can change the resulting robustness evaluation even for the same formula and trace.
For example, an evaluator often receives only a finite \emph{timed state sequence}
$(t_0, \vec{v}_0), \ldots, (t_n, \vec{v}_n)$ of timestamped samples, whereas
the reference semantics is defined over an idealized signal.
How this finite representation is interpolated into a signal over $\mathbb{R}_{\geq 0}$ is
an implicit semantic choice.
\Cref{tab:divergence} organizes the choices we consider.

\begin{example}[Verdict flip from one implicit choice]
\label{ex:verdict-flip}
Consider the \emph{signal beyond horizon} choice of \cref{tab:divergence}.
Let $\varphi = \Diamond_{[2,4]}(x \geq 0)$ be evaluated at $t = 0$ on a trace ending at $T = 1$ with $x(T) = 3$. The temporal window $[2,4]$ lies entirely beyond the trace. A clamping rule treats the last sample as persisting and reports $+3$ (satisfied), while a pessimistic rule treats the window as unavailable and reports $-\infty$ (violated). Thus, the same formula and trace can receive an opposite Boolean verdict depending on this choice.
\end{example}

This inconsistency, due to each tool's implicit semantic choices, is
particularly problematic when multiple tools are compared: in the ARCH-COMP
falsification competition, a tool's implicit choice of temporal and input bounds
invalidated the validation of \emph{other} tools' correct results, turning an
implicit choice into a fairness problem for the comparison
itself~\cite{ernst2021archcomp}.

% ================================================================
\section{Design and Implementation}
\label{sec:design}
% ----------------------------------------------------------------
% ================================================================

\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{figures/architecture.pdf}
\caption{Architecture of \tool: a stable core shared by every backend and a
swappable backend layer (lowering and node interpretation) that isolates the
implicit semantic choices.}
\label{fig:arch}
\end{figure}

This section turns the implicit semantic choices of \cref{sec:background} into
inspectable implementation structure. \tool exposes robustness evaluation
through a uniform interface, while placing choices that vary across evaluators
behind a backend boundary.

% ------------------------------------------------
\subsection{Evaluation Interface}

\tool has two explicit inputs: formula and signal.
A \emph{formula} is a typed syntax tree, parsed from the textual STL front end.
A \emph{signal} contains one or more named, batched value sequences over a shared time grid.

An optional third input, the \emph{backend}, encapsulates the implicit semantic
choices and the execution strategy. The call
\texttt{robustness(formula, signal, backend)} returns an array of robustness
values over time:

\begin{pycode}
from tidystl import Signal, parse, robustness
import numpy as np

phi = parse("G[0,5](F[0,2](velocity >= 10))")

times = np.linspace(0, 10, 200)
velocity_batch = np.array([np.sin(times), np.cos(times)])  # two traces
sig = Signal.from_dict(
    times=times,
    values={"velocity": velocity_batch},
)
rho = robustness(phi, sig)                    # default backend
rho = robustness(phi, sig, backend="breach")  # Breach-compatible
\end{pycode}

\noindent
Keeping this interface small serves two purposes. For users, the same formula
and signal can be evaluated under different documented backends without
changing the surrounding application. For backend authors, new evaluation
behavior attaches below a stable interface rather than requiring a new parser,
signal representation, or calling convention.

% ------------------------------------------------
\subsection{Architecture}

Internally, \tool separates a stable core from backend-specific evaluation logic, as shown in \cref{fig:arch}.
The stable core is shared by every backend, and it does not decide how $(\varphi, \sigma)$ maps to a robustness value.
Those decisions are delegated to the backend layer.

This separation provides \emph{observability}: because two backends build
their intermediate outputs on the same shared formula (syntax-tree)
representation, those outputs can be aligned at corresponding formula nodes.
This makes it possible to identify a minimal divergent node
and inspect the signal-processing operations responsible for it.
\Cref{sec:discrepancies} demonstrates this diagnostic workflow.

Each backend lowers the formula AST into a backend-specific computation DAG,
and then interprets its typed primitive operations as a concrete signal-processing
pipeline. This keeps signal-processing factors separate from the syntactic
and recursive handling of STL formulas.
\Cref{fig:inspectable-dag} illustrates
such a lowering on an example formula with nested temporal operators.

\begin{figure}[t]
\centering
\input{figures/inspectable-dag}
\caption{
Lowering of $\varphi :\equiv \Box_{[0,5]}\bigl((x \ge 0) \wedge
\Diamond_{[0,2]}(y \ge 0)\bigr)$ from AST (left) to computation DAG (right).
Each node is a primitive operation: the conjunction reduces pointwise while the
temporal operators aggregate over their annotated window. Edges carry
intermediate robustness signals that remain inspectable in the evaluation result.
}
\label{fig:inspectable-dag}
\end{figure}

\paragraph{Extension points.}

A backend consists of primitive operations, a lowering rule, and an executor;
extensions may add new primitives during lowering or override existing ones.
The artifact repository gives the user manual (\texttt{docs/usage.md}) and
reference implementations. In \cref{sec:implement-variations} we show that
emulating an existing tool requires only backend changes with little effort; a
performance sanity check finds the overhead of this structure negligible
(Appendix~\ref{sec:app-eval}).

\paragraph{Architectural byproducts.}
The swappable architecture (\cref{fig:arch}) also supports capabilities beyond
this paper's scope through the same interface: the codebase ships differentiable
(smooth robustness~\cite{kapoor2025stlcgpp}) and SIMD-optimized backends as
instances of this extensibility.


% ================================================================
\section{Empirical Study}
\label{sec:evaluation}
% ----------------------------------------------------------------
% ================================================================

\begin{table}[t]
\centering
\caption{Implicit-choice profile across three finite-trace axes
(cf.\ \cref{tab:divergence}) for Breach, TaLiRo, and RTAMT
(discrete and dense modes). \emph{Top}: the three diagnostic input signals,
named next to each row's test formula; samples are spaced evenly by index (the
$t$-axis gives each sample's real time) and the orange ring marks the readout
sample whose robustness the table reports. \emph{Bottom}: each row varies only
the named choice; each cell gives the implicit choice revealed at the readout sample,
with the observed value in parentheses. \emph{n/o}: choice not observable in
TaLiRo's whole-trace scalar output.}
\label{tab:divergence-profile}
\input{figures/divergence-signals}

\medskip

\input{figures/divergence-profile}
\end{table}

We evaluate \tool in two steps.\footnote{
The artifact repository includes the implementation used in our
empirical study, together with scripts and documentation (\texttt{extra/experiments/});
Appendix~\ref{sec:app-eval} records supplementary protocol and setup details.}
First, we show that realizing an implicit choice is a modular change confined to
the backend layer, and that the resulting compatibility backends faithfully
reproduce existing tools, up to one deliberately surfaced deviation
(\cref{sec:implement-variations}).
Second, building on these validated backends, we localize cross-tool
discrepancies to the responsible choice and formula node (\cref{sec:discrepancies}).

We fix a single set of reference tools throughout: Breach 1.11.4, RTAMT 0.3.5
(discrete default and dense, $\mathrm{RTAMT}_d$), and TaLiRo (SVN revision 146).
The implicit choices of \cref{sec:background} already produce discrepancies on
them: \cref{tab:divergence-profile} profiles three finite-trace axes, and every
one splits the tools into at least two camps, with no tool wrong.


% ================================================================
\subsection{Implementing Evaluation Variations as Modular Changes}
\label{sec:implement-variations}
% ----------------------------------------------------------------
% ================================================================


\begin{table}[t]
\centering
\caption{Implemented compatibility backends: layer changed and variant-specific
LOC (blank/comment lines excluded) as an auditable size proxy. All 103
regression tests pass with every variant installed.}
\label{tab:variation}
\footnotesize
\input{figures/variation-table}
\end{table}


We first measure the implementation cost of realizing an implicit choice in
\tool. \cref{tab:variation} shows that the shipped compatibility backends change
only backend-side components (\cref{fig:arch}): primitive operations, lowering
rules, and executors below the stable core.

\paragraph{Compatibility validation.}
Each reproduces its tool's robustness on the operator-coverage regression
suite---per-timestep for the Breach- and RTAMT-compatible backends, and at $t=0$
only for TaLiRo, which reports just that value---validated case by case against
the tool's committed reference fixtures by the artifact's reproduction test
suite (\cref{sec:app-eval}).


% ================================================================
\subsection{Localizing Cross-Tool Discrepancies}
\label{sec:discrepancies}
% ----------------------------------------------------------------
% ================================================================

\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{5pt}
\caption{Tool-vs-tool divergence localization with no ground truth. Each
  case lists the two tools compared, the formula size/operator depth, the root
  robustness each tool reports ($\rho$, left/right tool), and the localized
  divergence (the single origin operator the localizer blames). The two cases are
  \texttt{until\_boundary}, $(x \ge 0)\mathbin{\mathcal{U}_{[1,2]}}(y \ge 0)$, and
  \texttt{window\_sampling}, $\Box_{[0,3]}\bigl((\mathit{mode} \ge 0) \wedge
  \Diamond_{[0,1]}((\mathit{enable} \ge 0) \wedge \Box_{[1,2]}(\mathit{signal}
  \ge 0))\bigr) \wedge (\mathit{safety\_margin} \ge 0)$.}
\input{figures/localization_summary_table}
\label{tab:e2-localization}
\end{table}

\tool localizes each discrepancy in \cref{tab:divergence-profile} to the point
where the two aligned evaluations first diverge.
Using the validated backends of \cref{sec:implement-variations}, the localizer
compares a diverging pair node by node: every backend lowers the formula into
one shared syntax tree with inspectable per-node outputs, so a single post-order
walk reports a \emph{minimal divergent node}---a lowest node whose backend
outputs disagree while all of its children still agree---and the backend
primitive used to evaluate that node (\cref{tab:e2-localization}).

%\paragraph{Divergence without a verdict flip.}
Consider the \texttt{window\_sampling} case of \cref{tab:e2-localization}, the
conjunction
\[
  \Box_{[0,3]}\bigl((\mathit{mode} \ge 0) \wedge \Diamond_{[0,1]}((\mathit{enable}
  \ge 0) \wedge \Box_{[1,2]}(\mathit{signal} \ge 0))\bigr) \wedge
  (\mathit{safety\_margin} \ge 0).
\]
It can be read as a small CPS-style monitoring requirement with a mode signal,
an enabling signal, a monitored continuous signal, and a safety margin: during
the window $[0,3]$, the system must remain in the required mode; within the
initial window $[0,1]$, it must become enabled; once enabled, the monitored
signal must stay above its threshold throughout the following $[1,2]$ window;
and the run must maintain a nonnegative safety margin. We evaluate
it on a non-uniform trace whose grid jumps from $t=0$ straight to $t=3$ (samples
at $t = 0, 3, 4, \dots, 9$), with $\mathit{mode} = \mathit{enable} = 10$ and
$\mathit{safety\_margin} = 9$ held constant and
$\mathit{signal} = [3, 0, 5, \dots, 5]$.
Breach reports root robustness $+2.0$ and TaLiRo $+5.0$: both satisfy the spec,
yet the values differ, so no flipped verdict betrays the divergence.

The localizer pins the gap to the buried $\Box_{[1,2]}$, three temporal levels
down. Evaluated at the first step, this operator reduces $\mathit{signal}$ over
the window $[1,2]$, which lies entirely inside the $0 \to 3$ grid gap and so
contains no sample. Breach interpolates $\mathit{signal}$ at the window start and
returns $+2.0$ (because linear interpolation between the samples at $t=0$ and
$t=3$ gives $\mathit{signal}(1)=2$); TaLiRo finds the window empty and returns
the always-identity ($+\infty$), so that branch stops constraining the result.
These two local readings then drive the root through the three nesting levels
above: Breach's interpolated $+2.0$ propagates unchanged and stays binding at the
root, whereas TaLiRo's $+\infty$ makes that branch vacuous, so its root value is
instead set by the next active constraint one $\Box_{[0,3]}$ step over---the
window $[4,5]$, where $\mathit{signal}=5$---giving $+5.0$.
The cause lives entirely in how an off-grid temporal window is reduced;
a more readable walkthrough of this example is given in Appendix~\ref{sec:app-eval}.

%\paragraph{Localizing a discrepancy to its cause.}
The verdict can also flip outright. On the \texttt{until\_boundary} case, the
bounded-until formula $(x \ge 0)\mathbin{\mathcal{U}_{[1,2]}}(y \ge 0)$, Breach and RTAMT
disagree on the root
sign ($-5.0$ vs.\ $+2.0$; RTAMT is the lone outlier, with Breach and TaLiRo both
reporting $-5.0$), and the localizer pins every pairwise disagreement to the
single $\mathcal{U}_{[1,2]}$ node---RTAMT's discrete-time until composition. The analogous contamination in ARCH-COMP, by contrast, was traced
down only by manual investigation~\cite{ernst2021archcomp}; in both cases the
cause lives in the temporal semantics, which a reader cannot recover from the
formula alone.

% ================================================================
\section{Conclusion and Future Work}
\label{sec:conclusion}
% ----------------------------------------------------------------
% ================================================================
\tool makes the implicit choices of STL robustness evaluation explicit at the evaluator boundary: by separating formula syntax, signal data, backend lowering, and DAG interpretation, it exposes choices otherwise buried in tool internals and mechanically localizes cross-tool discrepancies to minimal divergent formula nodes and responsible operations.  This recasts robustness evaluation as an extensible semantic layer rather than an implementation detail of a particular monitor, falsifier, or learning pipeline; future work will broaden the supported semantics, including online evaluation, and expand the validation suite.

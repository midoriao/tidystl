% ================================================================
\section{Introduction}
\label{sec:intro}
% ----------------------------------------------------------------
% ================================================================

Signal Temporal Logic (STL) is a standard formalism for specifying temporal
properties of real-valued signals~\cite{maler2004monitoring,fainekos2009robustness}.
Its quantitative \emph{robustness} semantics measures how strongly a behavior
satisfies or violates a specification, and the resulting margin underpins runtime
monitoring~\cite{bartocci2018survey}, falsification~\cite{annpureddy2011staliro},
and learning-enabled design~\cite{raman2014mpc}.

The robustness semantics is defined over an idealized continuous signal, yet a tool
usually evaluates it on a finite trace---and a formula and trace do not determine a
robustness value on their own. Each tool fixes the rest, silently; we call this
\emph{implicit semantics} and organize the choices in \cref{tab:divergence}.
These choices are not cosmetic: they can flip a tool's verdict and propagate
downstream.
Prior cross-tool studies surfaced such disagreements by post-hoc independent
checking~\cite{ernst2021archcomp}; our work closes this gap, making the
responsible choice explicit and switchable before evaluation.

We present \tool,%
\footnote{Artifact repository: \url{https://github.com/midoriao/tidystl}.}
a diagnostic toolkit that does exactly this.
It places the implicit choices behind a
backend boundary, so the same formula and signal can be evaluated under named,
swappable backends while the parser, formula representation, and public interface
stay fixed (\cref{fig:arch}). Each backend exposes its choices as inspectable
intermediate computations; compatibility backends emulate existing tools for
cross-tool diagnosis, and the same separation makes the evaluator layer extensible
(\cref{tab:variation}).

This paper makes the following contributions.
\begin{itemize}
  \item We organize the implicit semantic choices in STL robustness
    evaluation and show that they materially affect acceptance, verdicts, and
    rankings, with documented consequences for cross-tool falsification
    benchmarking (\cref{sec:background,sec:discrepancies}).
  \item We present an architecture that represents these choices as explicit,
    swappable backends and makes their computations inspectable
    (\cref{sec:design}).
  \item Building on it, we emulate Breach~\cite{donze2010breach}, RTAMT~\cite{nickovic2020rtamt}, STLCG++~\cite{kapoor2025stlcgpp}, py-MTL~\cite{pyMTL}, and TaLiRo~\cite{annpureddy2011staliro} as compatibility backends
    and \emph{mechanically localize} their discrepancies to the responsible
    implicit choice and formula node (\cref{tab:divergence-profile,sec:discrepancies}).
  \item We show that the architecture provides an extensible evaluator layer:
    representative variations require modular backend changes while the
    formula, signal, and public interface remain fixed
    (\cref{sec:implement-variations}).
\end{itemize}

\paragraph{Related Work.}
In this paper, we focus on the comparison of \tool{} with Breach, RTAMT, STLCG++, py-MTL, and TaLiRo, which employ the quantitative STL semantics of Donz\'e and Maler~\cite{donze2010robust} or Fainekos and Pappas~\cite{fainekos2009robustness} as their underlying theory.
\cref{section:detailed_comparison} lists some other related tools.
Extending \tool{} to support other quantitative STL semantics, such as the RoSI semantics~\cite{deshmukh2017online}, is an interesting future direction.

% ================================================================
\section{Implicit Semantic Choices in STL Evaluation}
\label{sec:background}
% ----------------------------------------------------------------
% ================================================================

\begin{table}[t]
\centering
\caption{Representative \emph{implicit choices} in robustness evaluation~\cite{donze2010robust}.
Each choice can affect \emph{robustness} (the reported value) or \emph{acceptance} (class of accepted inputs), given the same formula and trace.
Appendix~\ref{sec:formal} further explores their scope.}
\label{tab:divergence}

\input{figures/choice-table.tex}
\end{table}


We take the quantitative STL semantics of Donz\'e and
Maler~\cite{donze2010robust} as a representative theoretical reference.
Together with the closely related framework by Fainekos and Pappas~\cite{fainekos2009robustness},
it represents the practical setting used by existing STL monitoring and falsification tools~\cite{ernst2021archcomp};
our focus is on evaluator-level variations within this setting.
To fix the scope, we recall only the definitions needed to delimit where
implicit semantic choices arise in evaluation.
For completeness, Appendix~\ref{sec:formal}
records the standard full inductive definition and further scope distinctions.

\begin{definition}[Signal]
\label{def:signal}
A \emph{signal} over a set of variables $\mathbf{Var}$ is a function
$\sigma \colon \mathbb{R}_{\geq 0} \to \mathbb{R}^{\mathbf{Var}}$.
\end{definition}

\begin{definition}[STL syntax]
\label{def:stl}
STL formulas $\varphi \in \mathbf{Fml}$ are given by:
\[
  \small
  \mathbf{Fml} \ni \varphi ::= {}
    \top \mid f(\vec{x}) \geq 0
    \mid \neg\varphi \mid \varphi_1 \vee \varphi_2
    \mid \varphi_1 \wedge \varphi_2
  \mid \varphi_1 \mathbin{U_I} \varphi_2
    \mid \varphi_1 \mathbin{R_I} \varphi_2
    \mid \Diamond_I\varphi \mid \Box_I\varphi .
\]
Here $f$ is a function symbol, for example $f(x, y) = 4x - y - 3$, and $I$ is a non-singular interval in $\mathbb{R}_{\geq 0}$, for example $[2,5]$, $(1,3]$, or $[0,\infty)$.
\end{definition}

\noindent
Informally, the robust semantics $\rho(\varphi, \sigma, t)$ replaces the Boolean
verdict with a signed real-value,
e.g., $\rho(x - 3 \geq 0, \sigma, t) = \sigma_x(t) - 3$,
which the connectives and temporal operators aggregate by $\min$, $\max$, $\inf$, and $\sup$, e.g.,
$\rho(\Box_{[0,2]}\,(x - 3 \geq 0), \sigma, t)
  = \inf_{t' \in [t,\,t+2]} (\sigma_x(t') - 3)$.

We call an evaluator decision an \emph{implicit semantic choice} if
it instantiates robustness evaluation in a way
not determined by the formal definition together with the given formula and trace,
and can change acceptance or reported robustness.
For example, an evaluator often receives only a finite \emph{timed state sequence}
$(t_0, \vec{v}_0), \ldots, (t_n, \vec{v}_n)$ of timestamped samples, whereas
the reference semantics is defined over an idealized signal.
How this finite representation is interpolated into a signal over $\mathbb{R}_{\geq 0}$ is
therefore an implicit semantic choice.
\Cref{tab:divergence} organizes the choices we consider.

\begin{example}[Verdict flip from one implicit choice]
\label{ex:verdict-flip}
Consider the \emph{signal beyond horizon} choice of \cref{tab:divergence}.
For $\varphi = \Diamond_{[2,4]}(x \geq 0)$ evaluated at $t = 1$ on a trace
ending at $T = 2$ with $x(T) = 3$, the window $[3,5]$ lies entirely past the
trace. A clamping rule treats the last sample as persisting and reports $+3$
(satisfied), while a pessimistic rule sees an empty window and reports
$-\infty$ (violated): same formula, same samples, opposite verdicts.
\end{example}

This is not a knife-edge coincidence of one trace: swept over a family of
inputs, the boundary- and terminal-step rules flip verdicts and reverse trace
rankings across a wide band of values (Appendix~\ref{sec:app-eval}).
The consequences compound when tools are compared. One tool's implicit choice
can contaminate another tool's verdict: in the ARCH-COMP falsification
competition, a tool's implicit choice of temporal and input bounds invalidated
the validation of \emph{other} tools' correct results, turning an implicit
choice into a fairness problem for the comparison
itself~\cite{ernst2021archcomp}.

% ================================================================
\section{Design and Implementation}
\label{sec:design}
% ----------------------------------------------------------------
% ================================================================

This section turns the implicit choices of \cref{sec:background} into
inspectable implementation structure. \tool exposes robustness evaluation
through a small interface, while placing choices that vary across evaluators
behind a backend boundary.

% ------------------------------------------------
\subsection{Evaluation Interface}

\tool has two explicit inputs: formula and signal.
A \emph{formula} is a typed syntax tree, parsed from the textual STL front end.
A \emph{signal} contains one or more named, batched value sequences over a shared time grid.

An optional third input, the \emph{backend}, encapsulates the implicit
choices and the execution strategy. Evaluation takes the form
\texttt{robustness(formula, signal, backend)} and returns an array of
robustness values over time:

\begin{pycode}
from tidystl import Signal, parse, robustness
import numpy as np

phi = parse("G[0,5](F[0,2](velocity >= 10))")

times = np.linspace(0, 10, 200)
velocity_batch = np.array([np.sin(times), np.cos(times)])  # two traces
sig = Signal.from_dict(
    times=times,
    values={"velocity": velocity_batch},
)
rho = robustness(phi, sig)                    # default backend
rho = robustness(phi, sig, backend="breach")  # Breach-compatible
\end{pycode}

% Signals are batch-first: a signal carries $B$ traces over a shared time
% grid, and \texttt{robustness} returns an $(B, T)$ array, so falsification-style
% workloads evaluate a whole batch in one call. Switching the backend
% is the one-argument change on the last line; the formula, the signal, and the
% calling code are untouched.

\noindent
Keeping this interface small serves two purposes. For users, the same formula
and signal can be evaluated under different documented backends without
changing the surrounding application. For backend authors, new evaluation
behavior attaches below a stable interface rather than requiring a new parser,
signal representation, or calling convention.

% ------------------------------------------------
\subsection{Architecture}

\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{figures/architecture.pdf}
\caption{Architecture of \tool. The stable core keeps formula and signal
inputs identical across comparisons; swappable conversion rule and node
interpretation make implicit choices explicit and their effects inspectable.}
\label{fig:arch}
\end{figure}

Internally, \tool separates a stable core from backend-specific evaluation logic, as shown in \cref{fig:arch}.
The stable core is shared by every backend, and it does not decide how $(\varphi, \sigma)$ maps to a robustness value.
Those decisions are delegated to the backend layer.

This separation provides \emph{observability}.

Since intermediate outputs from two different backends are built
on the same DAG representation,
their intermediate outputs can be aligned at corresponding formula nodes.
This makes it possible to identify the minimal divergent node
and inspect the signal-processing operations responsible for it.
\Cref{sec:discrepancies} demonstrates this diagnostic workflow.

Each backend lowers the formula AST into a backend-specific computation DAG,
and then interprets its typed primitive operations as a concrete signal-processing
pipeline. This keeps signal-processing factors separate from the syntactic
and recursive handling of STL formulas.
\Cref{fig:inspectable-dag} illustrates
such a lowering on an example formula with nested temporal operators.

\begin{figure}[t]
\centering
\input{figures/inspectable-dag}
\caption{
Lowering of $\varphi :\equiv \Box_{[0,5]}\bigl((x \ge 0) \wedge
\Diamond_{[0,2]}(y \ge 0)\bigr)$ from the formula AST (left) to a
computation DAG (right). Nodes are typed primitive operations
(\texttt{Min} is pointwise; \texttt{WMax}/\texttt{WMin} aggregate over the
annotated window); edges carry the intermediate robustness signals, which
remain inspectable in the evaluation result.
}
\label{fig:inspectable-dag}
\end{figure}

\paragraph{Extension points.}

A backend consists of primitive operations, a lowering rule, and an executor.
Extensions may introduce new primitive operations during lowering or override the interpretation of existing ones.
For programming details, the artifact repository gives the user manual (\texttt{docs/usage.md}) and reference implementations. In \cref{sec:implement-variations}, we demonstrate that emulating an existing tool's behavior only requires backend changes with small effort.
Despite this explicit structure, our performance sanity check found its overhead to be negligible
(Appendix~\ref{sec:app-eval}).

\paragraph{Architectural byproducts.}
The swappable architecture (\cref{fig:arch}) also supports capabilities outside
the central scope of this paper.
For example, it can accomodate advanced semantic extensions, such as smooth
differentiable robustness~\cite{levenberg2020stlcg,pant2017smooth}, as well as
performance-optimized backends, all through the same interface.
The codebase also contains differentiable and SIMD-optimized backends as instances of this extensibility.



% ================================================================
\section{Evaluation}
\label{sec:evaluation}
% ----------------------------------------------------------------
%% Purpose of the section:
%% Show that tidySTL is useful as an evaluator layer:
%% (1) variations are modular backend changes and backends are faithful;
%% (2) implicit choices cause observable discrepancies that tidySTL localizes.
%% Experiment campaign plan: notes/eval-plan.md (experiments E1-E5).
% ================================================================

We evaluate \tool in two steps.\footnote{The artifact repository provides reproduction instructions (\texttt{extra/experiments/}) for all experiments; Appendix~\ref{sec:app-eval} records supplementary protocol and setup details.}
First, we show that realizing an implicit choice in \tool is a modular change
confined to the backend layer, and that the resulting compatibility backends
faithfully reproduce existing tools (\cref{sec:implement-variations}).
Second, building on these validated backends, we show that implicit choices
produce real cross-tool discrepancies that \tool mechanically localizes to the
responsible choice and formula node (\cref{sec:discrepancies}).

% ================================================================
\subsection{Implementing Evaluation Variations as Modular Changes}
\label{sec:implement-variations}
% ----------------------------------------------------------------
%% Purpose:
%% Show that tidySTL's layered design allows for semantic and computational variations
%% under a common interface, with changes confined to the evaluator layer,
%% and that the compatibility backends faithfully reproduce existing tools.
%%
%% Figure/table:
%% - List of implemented variations with number of lines changed, regression tests. [E3]
%% - Cross-tool divergence matrix; the baseline-agreement cells are the validation claim. [E1]
%% - Performance sanity check is reported in the appendix.          [E4]
% ================================================================

% Appendix pointer:
% Regression suite details and expected-output generation.
% Tool-by-tool compatibility notes.
% Backend implementation notes.
% Additional benchmark results.

We first measure the implementation cost of realizing an implicit choice in
\tool: how much code changes below the stable interface.

\begin{table}[t]
\centering
\caption{Implemented variations. The key column is the layer changed; LOC
is reported only to make the modularity claim auditable, not as a quality
metric. The full
regression suite passes with all variants installed.}
\label{tab:variation}
\footnotesize
\input{figures/variation-table}
\end{table}

\paragraph{Implementation effort.}
\cref{tab:variation} shows that the shipped variants change only
backend-side components. Semantic variants change primitive operations,
lowering rules, or executors below the stable core; computational variants
keep the semantics fixed and change only execution. Thus modularity here
means a boundary of modification: the parser, formula representation,
signal layer, and user-facing interface are reused unchanged.

\paragraph{Faithful compatibility backends.}
\cref{tab:variation} accounts the three representative semantic variants
(Breach, RTAMT with discrete and dense modes, STLCG++); the same
backend-local mechanism yields two further compatibility backends used in the
profile below, py-metric-temporal-logic~\cite{pyMTL} (py-MTL) and
TaLiRo~\cite{annpureddy2011staliro}. Each reproduces its tool's per-timestep
robustness on the operator-coverage regression suite, validated case by case
against the tool's committed reference fixtures by the artifact's reproduction
test suite, with a single documented exception (\cref{sec:app-eval}). These validated backends, together
with the real tools' native outputs, are the instruments used in
\cref{sec:discrepancies} to profile and localize cross-tool discrepancies.

\begin{landscape}
\begin{table}
\centering
\caption{Implicit-choice profile of six real STL tools. For each implicit
choice (rows; cf.\ \cref{tab:divergence}) we run one diagnostic case,
constructed to vary only that choice, on each tool, read its per-timestep
robustness at the marked readout sample (orange ring in the signal
sparkline), and map it to the option the output reveals; the observed
robustness value is shown in parentheses. Tools: Breach 1.11.4, RTAMT 0.3.5
(discrete default and dense, $\mathrm{RTAMT}_d$), STLCG++ 0.0.2, and
py-metric-temporal-logic (py-MTL), with TaLiRo (dp\_taliro). \mbox{--}: the
tool cannot express the case (coverage); \emph{n/o}: the choice is real but
not observable in the tool's whole-trace scalar output (TaLiRo);
\emph{truncate}: a dense bounded operator past the horizon returns an empty
signal. The per-option prediction for each row is analytic (recorded in the
artifact), so each attribution is auditable from the printed value rather
than asserted.}
\label{tab:divergence-profile}
\resizebox{\linewidth}{!}{\input{figures/divergence-profile}}
\end{table}
\end{landscape}

% ================================================================
\subsection{Robustness Discrepancies and Their Localization}
\label{sec:discrepancies}
% ----------------------------------------------------------------
%% Purpose:
%% Demonstrate that implicit choices are not cosmetic, and that tidySTL
%% mechanically localizes each discrepancy to the responsible choice/node.
%%
%% Figure/table:
%% - Cross-tool divergence matrix (defined in sec:implement-variations) [E1]
%% - Divergence localization listing ("first divergent op")        [E2]
%% - Verdict-flip and falsification consequences are previewed in sec:background.
% ================================================================

Using the validated backends of \cref{sec:implement-variations}, we now show
that the implicit choices listed in \cref{tab:divergence} produce observable
cross-tool discrepancies, and that \tool localizes each discrepancy to the
responsible choice. We call this diagnostic capability \emph{divergence
localization}.

\paragraph{Real-tool discrepancies.}
We first profile the original tools directly. For each implicit choice we
construct one diagnostic case that varies only that choice, run it on each
real tool in its native environment, and read off the option the tool's
robustness output reveals. \cref{tab:divergence-profile} reports the result
across six tools: every implicit-choice axis splits the tools into at least
two camps. On \emph{signal beyond horizon}, for instance, Breach, dense
RTAMT, and STLCG++ clamp the last value ($+3$), discrete RTAMT and TaLiRo take
the pessimistic reading ($-\infty$), and py-MTL truncates the past-horizon
window entirely; this is the convention behind the verdict flip of
\cref{ex:verdict-flip}. The other axes split similarly---dense vs.\
discrete signal model, piecewise-linear vs.\ piecewise-constant
interpolation, exact vs.\ copy-previous robustness at the horizon, signed
vs.\ big-M equality, and signed vs.\ Euclidean distance. Because each row's
per-option prediction is analytic and printed alongside the observed value,
the attribution is auditable rather than asserted. The point is not that one
tool is correct and another is wrong, but that the same nominal input induces
different robustness behavior under different implicit choices.

\paragraph{Divergence localization.}
To inspect per-node outputs and to name the responsible implicit semantic
choice mechanically, the validated compatibility backends expose every
intermediate computation. On a diverging case, a localizer reports the
lowest syntax-tree node whose outputs disagree: on the \emph{signal beyond
horizon} case ($F_{[2,4]}$) it returns \texttt{WindowMax[2,4]} vs.\
\texttt{DiscreteWindowMax[2,4]}, naming the responsible choice from
\cref{sec:background} through the diagnostic architecture of
\cref{sec:design}. Thus the compatibility backends both reproduce the
observed outputs and expose the minimal divergent formula node and
responsible backend operations---exactly the diagnostic the falsification
community has lacked, where the contaminating bound choice noted in
\cref{sec:background} was traced down only by manual
investigation~\cite{ernst2021archcomp}.

\paragraph{Localizing buried causes.}
The same localizer applies to disagreements between real tools---with no
native reference and no syntactic giveaway. On the bounded-until case
$(x \ge 0)\mathbin{U_{[1,2]}}(y \ge 0)$, Breach and RTAMT flip the verdict
(root robustness $-5.0$ vs.\ $+2.0$); across four tools RTAMT is the lone
outlier (Breach, TaLiRo, and STLCG++ all report $-5.0$), and the localizer
pins every pairwise disagreement to the single $U_{[1,2]}$ operator
(\texttt{BoundedUntilKernel} vs.\ \texttt{DiscreteBoundedUntil}), naming
RTAMT's discrete-time until composition rather than anything in the syntax
(\cref{tab:e2-localization}). Divergences need not flip the verdict.
\Cref{fig:e2-window-sampling} shows a monitor three temporal levels deep on a
non-uniform grid in which a buried $G_{[1,2]}$ reduces over a window lying
entirely between samples: Breach interpolates at the window start while TaLiRo
finds no in-window sample and returns the always-identity, so both satisfy the
spec but differ in robustness ($+2.0$ vs.\ $+5.0$). The disagreement
propagates up the nesting, yet the localizer still reports the single origin
$G_{[1,2]}$ (\texttt{WindowMin} vs.\ \texttt{SampleMin}). (RTAMT and STLCG++
reject the non-uniform grid outright---a coverage finding---so only the
dense/sample-time tools apply.) In both cases the cause lives in the temporal
semantics, which a reader cannot recover from the formula alone.

\input{figures/localization_window_sampling}
\input{figures/localization_summary_table}

% % ================================================================
% \section{Related Work}
% \label{sec:relatedwork}
% % ----------------------------------------------------------------
% % ================================================================

% \Cref{sec:background} defines the semantic scope of this paper.
% \tool differs from Breach, S-TaLiRo, RTAMT, and similar systems
% not by replacing their workflows but by making the evaluator layer itself the
% primary object of extension and comparison~\cite{donze2010breach,donze2013efficient,annpureddy2011staliro,nickovic2020rtamt,nickovic2024rtamt}.
% A supplementary comparison of representative tool characteristics is provided
% in Appendix~\ref{sec:app-landscape}.

% Proof-oriented monitoring work~\cite{dokhanchi2014online,chattopadhyay2020verified}
% makes correctness guarantees for monitoring algorithms a central object.
% \tool does not claim comparable formal verification; instead it makes
% the implicit choices explicit so that divergence sources can be
% identified without proof.

% Speaking of implicitness is not a claim that tools such as RTAMT or Breach are
% poorly documented. The choices in question are simply not determined by the
% STL formula: adopting RTAMT, for instance, commits an evaluation to its
% discrete-time signal and pessimistic boundary rule regardless of the
% formula.
% The ARCH-COMP 2021 report~\cite{ernst2021archcomp} documented cross-tool
% discrepancies arising from exactly these implicit choices and
% handled them post hoc by independent checking, which surfaced the divergence
% without fully reconciling it; \tool instead makes the responsible choices
% explicit and switchable in advance.

% MoonLight~\cite{bartocci2023moonlight} shows that pluggable monitoring domains
% can be a first-class contribution; \tool similarly makes the implicit choices
% of STL robustness evaluation first-class.
% TeSSLa, RTLola, and HLola~\cite{kallwies2022tessla,finkbeiner2019rtlola,sanchez2021hlola}
% demonstrate that front-end/back-end separation is itself a scientific
% contribution; their backends vary in execution platform while \tool's vary
% in the implicit choices they realize.
% STLCG and related differentiable tools~\cite{levenberg2020stlcg,kapoor2025stlcgpp} occupy the
% smooth-semantics niche that the STLCG++-compatible backend in \tool targets
% for comparison.


% ================================================================
\section{Conclusion and Future Work}
\label{sec:conclusion}
% ----------------------------------------------------------------
% ================================================================
\tool makes the implicit choices of STL robustness evaluation explicit at the evaluator boundary.  By separating formula syntax, signal data, backend-specific lowering, and DAG interpretation, it exposes choices that are otherwise buried in tool internals and shows that they materially affect acceptance, verdicts, and rankings---with documented consequences for cross-tool falsification benchmarking---and that the resulting discrepancies can be mechanically localized to minimal divergent formula nodes and responsible operations.

The implementation supports the view of robustness evaluation as an extensible semantic layer rather than an implementation detail of a particular monitor, falsifier, or learning pipeline.  Future work will extend this layer to broader semantics, including online evaluation, and expand its validation suite.

% ================================================================
% ################################################################
% ================================================================
\section{Scope and Implicit Semantic Choices in Detail}
\label{sec:formal}

This appendix expands the implicit choices of
\cref{tab:divergence} and organizes the sources of cross-tool
difference. We first recall the reference robustness semantics, then detail
finite-trace interpretation, coverage, and implementation-specific behavior.
We then treat advanced semantic extensions, and close by stating what makes all
of these choices implicit.

% ------------------------------------------------
\subsection{Standard Robustness}

Following Donz\'e and Maler~\cite{donze2010robust}, fix a signal
$\sigma$ in the sense of \cref{def:signal} and a formula
$\varphi \in \mathbf{Fml}$ as in \cref{def:stl}. The standard quantitative
(robustness) semantics
$\rho(\varphi, \sigma, t) \in \mathbb{R} \cup \{\pm\infty\}$ is defined
inductively:
\begin{align*}
  \rho(\top,\sigma,t) &= +\infty \\
  \rho(f(\vec{x}) \geq 0,\sigma,t) &= f(\sigma_{\vec{x}}(t)) \\
  \rho(\neg\varphi,\sigma,t) &= -\rho(\varphi,\sigma,t) \\
  \rho(\varphi_1 \vee \varphi_2, \sigma, t) &= \max(\rho(\varphi_1,\sigma,t),\,\rho(\varphi_2,\sigma,t)) \\
  \rho(\varphi_1 \wedge \varphi_2, \sigma, t) &= \min(\rho(\varphi_1,\sigma,t),\,\rho(\varphi_2,\sigma,t)) \\
  \rho(\varphi_1 \mathbin{U_I} \varphi_2, \sigma, t) &= \sup_{t' \in t + I} \min\Bigl(\rho(\varphi_2,\sigma,t'),\, \inf_{t'' \in [t,\,t')} \rho(\varphi_1,\sigma,t'')\Bigr) \\
  \rho(\Diamond_I\varphi, \sigma, t) &= \sup_{t' \in t + I} \rho(\varphi,\sigma,t') \\
  \rho(\Box_I\varphi, \sigma, t) &= \inf_{t' \in t + I} \rho(\varphi,\sigma,t'),
\end{align*}
where $t + I = \{t + s \mid s \in I\}$ and $f(\sigma_{\vec{x}}(t))$ applies
$f$ to the values of the variables $\vec{x}$ at time $t$. The release
operator is defined by duality,
$\varphi_1 \mathbin{R_I} \varphi_2 :\equiv
 \neg(\neg\varphi_1 \mathbin{U_I} \neg\varphi_2)$,
and the constant $\bot :\equiv \neg\top$ has
$\rho(\bot,\sigma,t) = -\infty$.

Read literally, this definition assumes an idealized signal: continuous,
infinitely long, and known at every instant. An evaluator never has that. It
has a finite list of samples $\sigma(t_0), \dots, \sigma(t_n)$, and to compute the
$\sup$, $\inf$, and $\min$ above it must decide what the signal does at the
times that fall between, before, and after those samples. Those decisions
form the finite-trace interpretation discussed in \cref{sec:app-open}.

% ------------------------------------------------
\subsection{Coverage and Implementation Deviations}
\label{sec:app-corner}

Several rows of \cref{tab:divergence} are \emph{coverage} choices: adopting a
tool implicitly fixes which formulas it can evaluate at all, before any
robustness is computed.

\paragraph{Logic fragment.}
The until operator, with its nested $\inf$, is harder to vectorize than $\Box$
and $\Diamond$, so some tools support only the box/diamond fragment and drop
release with it. \cref{def:stl} also admits unbounded intervals such as
$[0,\infty)$ and an arbitrary function symbol $f$; many tools require a finite
upper bound and restrict predicates to threshold comparisons or linear
arithmetic. Each omission restricts the accepted language.

\paragraph{Interval endpoints and sampling grid.}
\cref{def:stl} admits open and half-open intervals such as $(1,3]$, but many
front ends parse only closed ones; where open intervals are accepted, excluding
an endpoint changes a discrete $\inf$/$\sup$ while leaving a continuous one
unchanged. A discrete-time evaluator addresses time by integer index, so it
admits only the integer-interval fragment: every bound must be an integer
multiple of the sampling period and the trace must be uniform. \tool's
RTAMT-compatible backend enforces this as a coverage restriction---on a
step-$1$ trace $\Box_{[1,2]}$ is admitted but $\Box_{[0.5,1.5]}$ is rejected
outright, and a non-uniform grid is rejected rather than silently snapped.

\paragraph{Robustness of $\top$ and $\bot$.}
The reference semantics fixes $\rho(\top) = +\infty$ and $\rho(\bot) = -\infty$.
Tools that substitute large finite sentinels deviate from it, and the sentinel
leaks into reported values once it is aggregated, scaled, or differentiated. We
record this as an implicit choice because adopting the evaluator silently
selects the effective behavior.

% ------------------------------------------------
\subsection{Implicit Semantic Choices for Finite Inputs}
\label{sec:app-open}

The most consequential category is the \emph{finite-trace interpretation}: the bundle of
implicit choices, summarized in \cref{tab:divergence},
by which the finite timed state sequence an evaluator receives is read as
the signal $\sigma$ that the semantics quantifies over. We discuss its
choices in turn, then the remaining implicit choices in
\cref{tab:divergence}.

\paragraph{Signal model.}
The signal model decides how a sampled trace is read as a function of
continuous time. Under a \emph{piecewise-linear} dense-time model (Breach, and
\tool's native backend) the value between two samples is their linear
interpolant, so a predicate such as $x \geq 0$ can change truth between sample
points. A \emph{piecewise-constant} model holds each sample until the next. A
\emph{discrete-time} model (RTAMT's default) drops the notion of between-sample
time altogether: the trace is an indexed sequence and temporal operators count
indices, not durations. The same formula and the same samples can therefore
denote different signals before evaluation even begins.

\paragraph{Window endpoints.}
Even under a fixed dense signal model, a temporal window $[t+a,\,t+b]$ whose
endpoints fall between samples leaves a choice of which points the
$\inf$/$\sup$ ranges over. \tool's native backend includes the interpolated
values at the exact endpoints alongside the in-window samples, returning the
true piecewise-linear extremum over the real interval. The Breach-compatible
backend, by contrast, reduces over the in-window \emph{samples only} whenever
the window contains at least one sample, and does not add the interpolated
endpoint values to that reduction. When the window falls entirely between two
samples and so contains none, it instead reads a single value: the signal
interpolated at the window start at the first evaluated step, or the current
sample's value at later steps (both pinned to Breach ground truth). Both
backends are piecewise-linear, yet they disagree on any off-grid window: for
$\Diamond_{[0.3,0.7]}(v \geq 5)$, evaluated where the window lies between
samples, the native backend takes the true extremum over the real interval and
reports $-0.2$, while the Breach-compatible backend returns the window-start
interpolant $-1.0$. \tool records this among the evaluator-specific
behaviors of \cref{tab:divergence}, as it reflects a Breach implementation
convention rather than a primary finite-trace choice.

The \emph{signal beyond horizon} and \emph{robustness at horizon} rows of
\cref{tab:divergence} cover the next two choices: how a window reaching past
the trace end is filled, and how the final sample is reported.

\paragraph{Signal beyond horizon (boundary rule).}
When a window $[t+a,\,t+b]$ reaches past the end of the trace at $T$, the
$\sup$ or $\inf$ ranges in part over times where no sample exists. The boundary
rule supplies those values, either by \emph{extending} the signal past the
horizon or by leaving the past-end region \emph{empty}---the \emph{Extend} and
\emph{Empty} entries in \cref{tab:divergence}. Under \emph{clamp} (\tool, Breach) the last
sample is treated as persisting, so an out-of-range query returns $\sigma(T)$. Under
a \emph{pessimistic} rule (RTAMT discrete, TaLiRo) the out-of-range part contributes
nothing: the window is effectively empty, so a $\Diamond$ becomes $-\infty$
(the $\sup$ of an empty set) and a $\Box$ becomes $+\infty$.
RTAMT's dense mode ($\mathrm{RTAMT}_d$), by contrast, extends the final sample,
matching the clamp rule rather than the discrete mode's pessimistic one.
This is the \emph{signal beyond horizon} row of \cref{tab:divergence-profile},
illustrated on a concrete trace in \cref{ex:verdict-flip}. It is a sign change
in the reported value, not a difference in input acceptance---both backends
evaluate the formula and only the verdict differs---so, like the signal model,
it is a robustness effect.

\paragraph{Robustness at horizon (terminal-step rule).}
At the final sample $t_N$ a tool may report the \emph{exact} robustness it
computed there, or it may \emph{copy} the value from the penultimate sample
$t_{N-1}$---the \emph{Exact} and \emph{Copy-prev} entries in \cref{tab:divergence}.
Breach does the latter by default; RTAMT does the former. The two
agree whenever the final and penultimate robustness coincide, so the choice is
silent on most traces but not when they differ. The \emph{robustness at horizon}
row of \cref{tab:divergence-profile} shows such a trace, where the penultimate
$+5$ and final $-3$ part the two rules; against a constant $+0.1$ trace this even
flips which trace looks better, reversing a ranking that a falsification search
would act on.

\paragraph{Robustness of atomic predicates.}
Even for an accepted predicate, the evaluator must decide how it is scored
(the \emph{distance metric} row of \cref{tab:divergence}):
the robustness of $f(\vec{x}) \geq 0$ may be the raw signed margin
$f(\vec{x})$ or a Euclidean distance to the satisfaction
set~\cite{fainekos2009robustness}, and the two disagree once a predicate
involves several variables.

% ------------------------------------------------
\subsection{Advanced Semantic Extensions}
\label{sec:app-ext}

Applications often demand more than \cref{def:stl} provides. The extensions
below select a different language or evaluation problem; whether a tool supports
one is itself a coverage choice.

\paragraph{Equality predicates.}
The equality $x = 0$ is absent from \cref{def:stl} yet common in practical
specifications. Once a tool accepts it the assigned robustness is an implicit
choice (the \emph{nonstandard predicates} row of \cref{tab:divergence}): a
common reading is $-|x|$, which is never positive, so even a satisfied equality
reports non-positive robustness, while Breach uses a fixed-magnitude sign
convention ($\pm 10^4$) that carries no metric information.

\paragraph{Other extensions.}
Past-time operators (once, historically), differentiable robustness that
replaces $\min$/$\max$ by smooth
approximations~\cite{levenberg2020stlcg,pant2017smooth}, and online evaluation
over growing prefixes~\cite{deshmukh2017online} each change the language or the
evaluation problem; \tool exposes the smooth case explicitly as a selectable
backend.

% ------------------------------------------------
\subsection{Why These Choices Are Implicit}

Across the four groups of \cref{tab:divergence}, one property recurs: each
choice is fixed not by the STL formula or the sampled trace but by the act of
adopting an evaluator, which is what makes it implicit and invisible at the
specification level. The extensions of \cref{sec:app-ext} sharpen this boundary
rather than crossing it---selecting, say, smooth robustness makes that one
choice visible while the tool's surrounding behavior stays implicit.
``Choice'' is descriptive: it names the effective behavior without judging
whether a tool fixed it deliberately, and its practical consequence is what
\cref{sec:discrepancies} measures---two tools whose combinations differ can
reject different inputs, report different robustness, or return opposite
verdicts on identical input.

% ================================================================
\section{Experimental Details}
\label{sec:app-eval}
% ================================================================

This appendix records the protocols and full data behind the findings
reported in \cref{sec:evaluation}.

\paragraph{Artifact and reproducibility.}
The public \href{https://github.com/midoriao/tidystl}{artifact repository}
contains the implementation, fixtures, and reference-tool adapters. The
\href{https://github.com/midoriao/tidystl/blob/main/packages/tidystl/docs/usage.md}{user manual}
documents installation, backend semantics, and worked examples; the
\href{https://github.com/midoriao/tidystl/blob/main/extra/experiments/README.md}{experiment guide}
documents how to regenerate every evaluation record, table, and figure.

\paragraph{Diagnostic case set and protocol.}
The profile of \cref{tab:divergence-profile} uses one diagnostic case per
implicit choice, each a small formula and trace constructed so that only that
choice can affect the reported robustness. Every case runs on each tool in its
native environment---Breach (from a committed MATLAB cache), RTAMT in its
discrete and dense modes, and TaLiRo (\texttt{dp\_taliro})---and we read the
tool's per-timestep robustness at the marked readout sample and map it to the
option that output reveals. The per-option robustness signature is predicted
analytically and printed beside the observed value, so each attribution is
auditable rather than asserted. TaLiRo's \texttt{dp\_taliro} reports only a
whole-trace scalar at $t=0$, so a choice that acts only on a later sample is
marked \emph{n/o} (not observable) rather than guessed.

\paragraph{Compatibility validation.}
Each \tool compatibility backend is validated separately against its tool's
committed reference fixtures---real-tool robustness regenerated from Breach,
RTAMT, and TaLiRo---by the artifact's per-tool reproduction
test suite at absolute tolerance $10^{-6}$ on the operator-coverage regression
suite. The check is per-timestep for the Breach- and RTAMT-compatible backends;
for TaLiRo it is at $t=0$ only, the single value \texttt{dp\_taliro} reports.
The backends implement each tool's \emph{documented} behavior where it is
specified and, where it is not, the behavior pinned by these committed
reference fixtures; the check therefore verifies the implementations rather than
defining the rules. The one case where a backend departs from its
reference tool is itself an implicit choice this paper aims to surface: where
real RTAMT silently reinterprets a non-uniform grid as uniformly indexed
samples, the RTAMT-compatible backend makes this an \emph{explicit} coverage
rejection rather than reproducing the silent reinterpretation (cf.\ the
coverage choices of \cref{sec:app-corner}).

\paragraph{Localization procedure.}
Across any pair of validated backends, the localizer exploits that \tool
backends share one formula representation with inspectable per-node outputs:
it walks the shared syntax tree in post-order and reports every \emph{minimal
divergent node}---a node whose outputs disagree while all its children
agree---together with each backend's primitive operation at that node. To
illustrate the output format, on two minimal single-operator cases (the
\emph{signal beyond horizon} and \emph{robustness at horizon} choices) the
listing reads:
\begin{lstlisting}[language={},keywordstyle={},basicstyle=\footnotesize\ttfamily]
first divergent op at `F[2,4]`: WindowMax[2,4] vs
    DiscreteWindowMax[2,4] (t-index 1) -> boundary rule
first divergent op at `and`: PointwiseMin vs PointwiseMin
    + terminal-step extension (t-index 2) -> terminal-step rule
\end{lstlisting}
The headline results are the tool-vs-tool localizations of
\cref{sec:discrepancies} (\cref{tab:e2-localization,fig:e2-window-sampling}),
where the same post-order walk pins a cause buried under several nesting levels.
Backend pairs whose lowerings differ structurally are compared at the
shared syntax-tree level rather than between lowered representations;
predicate-internal arithmetic is treated as a leaf, since none of the
implicit choices studied here concerns its internal operations.

\begin{figure}[ht]
\centering
\input{figures/localization_window_sampling}
\caption{Tool-vs-tool localization on \texttt{div\_window\_sampling}
  (\textsc{Breach} vs.\ \textsc{TaLiRo}, no ground truth; discussed in
  \cref{sec:discrepancies}). The buried $\Box_{[1,2]}$ reduces over a window
  lying entirely between samples on a non-uniform grid: \textsc{Breach}
  interpolates at the window start while \textsc{TaLiRo} returns the
  always-identity. Shaded nodes mark the divergence propagation path up to the
  single origin $\Box_{[1,2]}$
  ($\mathtt{WindowMin}$ vs.\ $\mathtt{SampleMin}$), annotated in the dashed box;
  both satisfy the spec ($+2.0$ vs.\ $+5.0$).}
\label{fig:e2-window-sampling}
\end{figure}

\paragraph{Trace-family construction.}
A single constructed trace could sit on a knife edge, so each headline
case is widened to a swept family of 201 traces whose final sample varies
over $[-5,5]$, spanning regions of agreement and disagreement; no rule is
taken as the reference, and the rates are symmetric between the rules by
construction. For the boundary-rule family ($\Diamond_{[2,4]}(x \ge 0)$,
window past the trace end), the clamping and pessimistic rules return
opposite verdicts at the divergence timestep on exactly those traces whose
final sample is non-negative (101 of 201). The terminal-step family is a
conjunction violated only at the final sample; the as-is and
extend-penultimate rules flip the final verdict on 100 of 201 traces and
rank the trace oppositely against a fixed reference trace with an interior
violation---the ranking signal a falsification search acts on---on 80 of
201. These rates measure the width of the disagreement region within
each controlled family, not a frequency claim about any particular workload.

\paragraph{Variant accounting.}
LOC in \cref{tab:variation} counts variant-specific code only: blank,
comment-only, and docstring lines are excluded, and shared infrastructure is
excluded. The native backend is the baseline and has no row. The full
regression suite comprises 103 tests and passes with all variants installed.

\paragraph{Performance sanity check.}
The benchmark in \cref{fig:scaling} compares \tool backends with real RTAMT and STLCG++ on
the same workload. On single traces, the explicit evaluator layer remains
within the same order of magnitude as the Python-stack references. On
batches, \tool's batch-first signal layout amortizes per-trace cost, making
the native backend substantially faster than looping RTAMT and faster than
the tensor-batched STLCG++ run shown here. The SIMD executor gives the
expected speed ceiling once per-node observability is dropped. This check
shows that inspectability does not impose prohibitive overhead; performance
is not a primary evaluation claim.

\begin{figure}[ht]
\centering
\input{figures/scaling-plot}
\caption{Mean evaluation time (log scale) on
$\Box_{[0,5]}((x>0)\wedge\Diamond_{[0,2]}(y>0))$; error bars are one
standard deviation. RTAMT$^\dagger$: batch time is $N \times$ per-trace time.
Missing STLCG++ bars: OOM.}
\label{fig:scaling}
\end{figure}

\Cref{tab:scaling} tabulates the measurements plotted in
\cref{fig:scaling}.

\begin{table}[ht]
\centering
\caption{Mean evaluation time (ms) $\pm$ std on the same workload as
\cref{fig:scaling}. $\dagger$: RTAMT is single-trace; batch times are
$N \times$ per-trace (linearity checked at $N \in \{1,8\}$).
OOM: window materialization exceeds memory on the 32\,GB host.}
\label{tab:scaling}
\input{figures/scaling-table}
\end{table}


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Comparison with Other Tools}\label[appendix]{section:detailed_comparison}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%-----------------------------------------------------------
\begin{table}[tp]
 \centering
 \caption{Tools supporting STL or related robustness monitoring.
  ``classical'' semantics: \cite{donze2010robust} or \cite{fainekos2009robustness};
  ``RoSI-style'': \cite{deshmukh2017online}.}
 \label{table:detailed_tool_comparison}
 \footnotesize
 \begin{tabular}{ccc}
  \toprule
  \multirow{2}{*}{Name} & \multirow{2}{*}{Supported Logic} & Supported \\
          &       & Nominal Semantics    \\\midrule
  Breach~\cite{donze2010breach}  & Future-time STL & classical \\
  TaLiRo~\cite{annpureddy2011staliro}  & Future-time STL & classical \\
  RTAMT~\cite{nickovic2024rtamt} & Future- and past-time STL & classical \\
  STLCG++~\cite{kapoor2025stlcgpp} & Future-time STL & classical and smooth~\cite{levenberg2020stlcg,pant2017smooth} \\
  SignalTemporalLogic.jl\footnote{https://github.com/sisl/SignalTemporalLogic.jl} & Future-time STL & classical and smooth~\cite{levenberg2020stlcg,pant2017smooth} \\
  py-signal-temporal-logic~\cite{pyMTL} & Future-time STL & classical \\
  mstlo~\cite{thomsen2026mstlo} & Future-time STL & classical and RoSI-style \\
  Argus\footnote{\url{https://github.com/anand-bala/argus}}   & Future-time STL & classical \\
  STLRom~\footnote{\url{https://github.com/decyphir/STLRom}}  & Future-time STL & RoSI-style \\
  % Reelay~\cite{reelay}  & Past-time STL & Boolean \\
  CauMon~\cite{CauMon}  & Future-time STL & causation semantics~\cite{DBLP:conf/cav/ZhangAAH23} \\
  MoonLight~\cite{bartocci2023moonlight} & STREL~\cite{DBLP:conf/memocode/BartocciBLN17} & classical \\
  \multirow{2}{*}{TempLogic\footnote{\url{https://github.com/fran-penedo/templogic}}}  & Future-time STL, & \multirow{2}{*}{classical} \\
  & TSSL~\cite{DBLP:journals/tcns/BartocciGHB18}, SpaTeL~\cite{DBLP:conf/hybrid/HaghighiJKBGB15} & \\\bottomrule
 \end{tabular}
\end{table}
%-----------------------------------------------------------

\cref{table:detailed_tool_comparison} lists further tools that monitor the
robustness of STL or related logics, classified by their supported nominal
semantics (defined in the caption). \tool{} currently supports the classical and
smooth semantics; extending it with compatibility backends for further
classical-semantics tools, and for the RoSI-style~\cite{deshmukh2017online} and
causation~\cite{DBLP:conf/cav/ZhangAAH23} semantics, is future work that the
modular structure of \cref{sec:implement-variations} should make
straightforward.

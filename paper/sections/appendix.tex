% ================================================================
% ################################################################
% ================================================================
\section{Scope and Implicit Semantic Choices in Detail}
\label{sec:formal}

This appendix expands the implicit choices of
\cref{tab:divergence} and organizes the sources of cross-tool
difference. We first recall the reference robustness semantics, then detail
finite-trace interpretation, coverage, and implementation-specific behavior.
We finally distinguish explicit selection of advanced semantic
extensions from implicit tool behavior around them.

% ------------------------------------------------
\subsection{Standard Robustness}

Following Donz\'e and Maler~\cite{donze2010robust}, fix a signal
$\sigma$ in the sense of \cref{def:signal} and a formula
$\varphi \in \mathbf{Fml}$ as in \cref{def:stl}. The standard quantitative
(robustness) semantics
$\rho(\varphi, \sigma, t) \in \mathbb{R} \cup \{\pm\infty\}$ is defined
inductively:
\begin{align*}
  \rho(\top,\sigma,t) &= +\infty \\
  \rho(f(\vec{x}) \geq 0,\sigma,t) &= f(\sigma_{\vec{x}}(t)) \\
  \rho(\neg\varphi,\sigma,t) &= -\rho(\varphi,\sigma,t) \\
  \rho(\varphi_1 \vee \varphi_2, \sigma, t) &= \max(\rho(\varphi_1,\sigma,t),\,\rho(\varphi_2,\sigma,t)) \\
  \rho(\varphi_1 \wedge \varphi_2, \sigma, t) &= \min(\rho(\varphi_1,\sigma,t),\,\rho(\varphi_2,\sigma,t)) \\
  \rho(\varphi_1 \mathbin{U_I} \varphi_2, \sigma, t) &= \sup_{t' \in t + I} \min\Bigl(\rho(\varphi_2,\sigma,t'),\, \inf_{t'' \in [t,\,t')} \rho(\varphi_1,\sigma,t'')\Bigr) \\
  \rho(\Diamond_I\varphi, \sigma, t) &= \sup_{t' \in t + I} \rho(\varphi,\sigma,t') \\
  \rho(\Box_I\varphi, \sigma, t) &= \inf_{t' \in t + I} \rho(\varphi,\sigma,t'),
\end{align*}
where $t + I = \{t + s \mid s \in I\}$ and $f(\sigma_{\vec{x}}(t))$ applies
$f$ to the values of the variables $\vec{x}$ at time $t$. The release
operator is defined by duality,
$\varphi_1 \mathbin{R_I} \varphi_2 :\equiv
 \neg(\neg\varphi_1 \mathbin{U_I} \neg\varphi_2)$,
and the constant $\bot :\equiv \neg\top$ has
$\rho(\bot,\sigma,t) = -\infty$.

Read literally, this definition assumes an idealized signal: continuous,
infinitely long, and known at every instant. An evaluator never has that. It
has a finite list of samples $\sigma(t_0), \dots, \sigma(t_n)$, and to compute the
$\sup$, $\inf$, and $\min$ above it must decide what the signal does at the
times that fall between, before, and after those samples. Those decisions
form the finite-trace interpretation discussed in \cref{sec:app-open}.

% ------------------------------------------------
\subsection{Coverage and Implementation Deviations}
\label{sec:app-corner}

\paragraph{Support for until and release.}
Few tools implement the full syntax of \cref{def:stl}. The until operator,
with its nested $\inf$, is harder to implement and to vectorize than $\Box$
and $\Diamond$, and release is often dropped together with it: some tools
support only the box/diamond fragment, others support until under
restricted interval forms. The effect is on coverage: a specification
accepted by one tool is rejected by another before any robustness is
computed. This is a syntax-coverage choice: adopting the tool implicitly
selects which formulas can be evaluated.

\paragraph{Unbounded temporal operators.}
\cref{def:stl} admits unbounded intervals such as $[0,\infty)$, so $\Box$,
$\Diamond$, and until quantify over the whole remaining horizon. Many tools
require a finite upper bound and reject the unbounded forms, or accept them
only by silently truncating the window to the trace end. This too is a
syntax-coverage choice: the logic fixes the construct, and an evaluator that
omits it restricts to the bounded fragment.

\paragraph{Range of expressible atomic predicates.}
\cref{def:stl} permits an arbitrary function symbol $f$, but front ends
restrict what is expressible: to threshold comparisons on single variables,
to linear arithmetic, or to a fixed expression grammar. This is also a
coverage choice.

\paragraph{Open vs.\ closed interval endpoints.}
\cref{def:stl} admits open and half-open intervals such as $(1,3]$, but many
front ends parse only closed ones. Where open intervals are accepted, their
reading depends on the signal interpretation: under a continuous interpolant
the $\inf$ over an open window coincides with that over its closure, so the
difference is invisible, while under a discrete reading excluding an
endpoint drops a sample and can change the aggregated value. Rejecting an
open interval is a coverage choice; silently treating it as closed is an
implementation deviation. Both are implicit because the formula does not
announce how the evaluator will handle it.

\paragraph{Sampling-grid constraints.}
A discrete-time evaluator addresses time by integer sample index, so under an
appropriate time scaling it admits only the integer-interval fragment: every
temporal-interval bound must be an integer multiple of the sampling period
(and the trace must be uniformly sampled). \tool's RTAMT- and
STLCG++-compatible backends enforce this as a coverage restriction---they
reject a non-uniform grid, and they reject a non-commensurate interval rather
than silently snapping it. On an integer-time trace (step $1$), $\Box_{[1,2]}$
is admitted but $\Box_{[0.5,1.5]}$ is rejected outright. Like the other
fragment choices this is an input-acceptance effect; it manifests as the
discrete tools' coverage entries (\mbox{--}) on the interpolation row of
\cref{tab:divergence-profile}.

\paragraph{Robustness of $\top$ and $\bot$.}
The standard semantics assigns $\rho(\top,\sigma,t) = +\infty$ and
$\rho(\bot,\sigma,t) = -\infty$. Tools differ in whether they admit infinite
values at all: some propagate genuine infinities through the $\min$/$\max$
aggregation, others substitute large finite sentinels, and a sentinel leaks
into reported values once it is aggregated, scaled, or differentiated. Since
the reference semantics fixes genuine infinities, a finite sentinel is an
implementation deviation. It is nevertheless an implicit semantic choice in
our taxonomy because adopting the evaluator silently selects this effective
behavior.

% ------------------------------------------------
\subsection{Implicit Semantic Choices for Finite Inputs}
\label{sec:app-open}

The most consequential category is the \emph{finite-trace interpretation}: the bundle of
implicit choices, summarized in \cref{tab:divergence},
by which the finite timed state sequence an evaluator receives is read as
the signal $\sigma$ that the semantics quantifies over. We discuss its
choices in turn, then the remaining implicit choices in
\cref{tab:divergence}.

\paragraph{Signal model.}
The signal model decides how a sampled trace is read as a function of
continuous time. Under a \emph{piecewise-linear} dense-time model (Breach, and
\tool's native backend) the value between two samples is their linear
interpolant, so a predicate such as $x \geq 0$ can change truth between sample
points. A \emph{piecewise-constant} model holds each sample until the next. A
\emph{discrete-time} model (RTAMT's default) drops the notion of between-sample
time altogether: the trace is an indexed sequence and temporal operators count
indices, not durations. The same formula and the same samples can therefore
denote different signals before evaluation even begins.

\paragraph{Window endpoints.}
Even under a fixed dense signal model, a temporal window $[t+a,\,t+b]$ whose
endpoints fall between samples leaves a choice of which points the
$\inf$/$\sup$ ranges over. \tool's native backend includes the interpolated
values at the exact endpoints alongside the in-window samples, returning the
true piecewise-linear extremum over the real interval. The Breach-compatible
backend reduces over the in-window \emph{samples only} and never interpolates
at an endpoint (pinned to Breach ground truth). Both backends are
piecewise-linear, yet they disagree on any off-grid window: for
$\Diamond_{[0.3,0.7]}(v \geq 5)$ the native backend reports $-0.2$ and the
Breach-compatible one $-1.0$. This is less a principled semantic choice than a
Breach implementation convention---the samples-only reduction merely omits the
exact endpoint---so \tool records it among the evaluator-specific behaviors in
\cref{tab:divergence} rather than as a primary finite-trace choice, even though
it still shifts the reported robustness.

The \emph{signal beyond horizon} and \emph{robustness at horizon} rows of
\cref{tab:divergence} cover the next two choices: how a window reaching past
the trace end is filled, and how the final sample is reported.

\paragraph{Signal beyond horizon (boundary rule).}
When a window $[t+a,\,t+b]$ reaches past the end of the trace at $T$, the
$\sup$ or $\inf$ ranges in part over times where no sample exists. The boundary
rule supplies those values, either by \emph{extending} the signal past the
horizon or by leaving the past-end region \emph{empty}---the \emph{Extend} and
\emph{Empty} entries in \cref{tab:divergence}. Under \emph{clamp} (\tool, Breach) the last
sample is treated as persisting, so an out-of-range query returns $\sigma(T)$. Under
a \emph{pessimistic} rule (RTAMT discrete, TaLiRo) the out-of-range part contributes
nothing: the window is effectively empty, so a $\Diamond$ becomes $-\infty$
(the $\sup$ of an empty set) and a $\Box$ becomes $+\infty$. A third rule,
\emph{last-value extension} (STLCG++ with \texttt{padding=last}), repeats the
final sample to fill a truncated window in index space; it agrees with clamp
in spirit but is computed on the discrete grid. A fourth behavior,
\emph{truncation} (py-MTL), drops the past-horizon region and returns an empty
signal there rather than a value.
This is the \emph{signal beyond horizon} row of \cref{tab:divergence-profile},
illustrated on a concrete trace in \cref{ex:verdict-flip}. It is a sign change
in the robustness value, not a difference in input acceptance: both backends
evaluate the formula, and only the reported value---and hence the
verdict---differs. The \emph{signal beyond horizon} rule is therefore a
robustness effect, like the signal model.

\paragraph{Robustness at horizon (terminal-step rule).}
At the final sample $t_N$ a tool may report the \emph{exact} robustness it
computed there, or it may \emph{copy} the value from the penultimate sample
$t_{N-1}$---the \emph{Exact} and \emph{Copy-prev} entries in \cref{tab:divergence}.
Breach does the latter by default; RTAMT does the former. The two
agree whenever the final and penultimate robustness coincide, so the choice is
silent on most traces. It is not silent when they differ.
This is the \emph{robustness at horizon} row of \cref{tab:divergence-profile}. For
$\varphi = (x \geq 0) \wedge (y \geq 0)$ on trace~A with $x = y = [5, 5, -3]$,
the penultimate robustness is $+5$ and the final is $-3$. The standard rule
reports $-3$; the extension rule copies the penultimate $+5$. Against a
trace~B that holds $+0.1$ throughout, this flips which trace looks better,
reversing a ranking that a falsification search would act on.

\paragraph{Robustness of atomic predicates.}
Even for an accepted predicate, the evaluator must decide how it is scored
(the \emph{distance metric} row of \cref{tab:divergence}):
the robustness of $f(\vec{x}) \geq 0$ may be the raw signed margin
$f(\vec{x})$ or a Euclidean distance to the satisfaction
set~\cite{fainekos2009robustness}, and the two disagree once a predicate
involves several variables.

% ------------------------------------------------
\subsection{Advanced Semantic Extensions}
\label{sec:app-ext}

Applications genuinely demand more than \cref{def:stl} provides. The
following extensions explicitly select a different language or evaluation
problem. That selection is not implicit when exposed to the user; whether a
tool supports the extension is an implicit coverage choice, and any
tool-fixed behavior within the selected extension remains an implicit choice.

\paragraph{Equality predicates.}
The equality $x = 0$ is absent from \cref{def:stl} yet common in practical
specifications. Adding equality to the language is an explicit extension;
once a tool accepts it, however, the assigned robustness is an implicit
choice because the formula does not select one (the \emph{nonstandard
predicates} row of \cref{tab:divergence}). Equality is satisfied only
at the single value $x = 0$ and admits no single standard robustness; a
common choice is $-|x|$, which is never positive, so even a satisfied
equality reports non-positive robustness.
Breach instead scores equality by a fixed-magnitude sign convention
($\pm 10^4$), so the reported value carries no metric information at
all---the \emph{equality predicate} row of \cref{tab:divergence-profile} shows the
resulting cross-tool divergence on inputs every tool accepts.

\paragraph{Past-time operators.}
Operators such as once and historically mirror the temporal modalities into
the past. They change the direction in which an evaluator walks the trace,
and tools either provide them (RTAMT) or not. The effect is on coverage.

\paragraph{Differentiable robustness.}
Gradient-based learning replaces the $\min$/$\max$ aggregations by smooth
approximations~\cite{levenberg2020stlcg,pant2017smooth}. The smooth value
deviates from the standard robustness by construction. Whether the deviation
is exposed as a tunable parameter or buried in the tool determines its
classification: explicitly selecting smooth robustness changes the evaluation
problem, while a buried approximation is implicit implementation behavior.

\paragraph{Online evaluation.}
Online monitoring evaluates a growing prefix, so verdicts must be issued
before the trace is complete~\cite{deshmukh2017online}. This forces a
treatment of missing future information---three-valued verdicts, robustness
intervals---that offline evaluation never confronts.

% ------------------------------------------------
\subsection{Why ``Implicit''}

The choices in \cref{tab:divergence} are implicit because they
are absent from both the STL formula and the finite sampled input, yet are
fixed by adopting the evaluator. Choosing a tool therefore silently chooses a
combination of finite-trace conventions, scoring rules, coverage boundaries,
and implementation behavior. ``Choice'' here names that effective behavior
without judging whether it is deliberate. Two tools whose
combinations differ can reject different inputs, report different robustness,
or even produce opposite verdicts on identical input. The contribution of
\tool is not to declare one combination correct, but to expose and diagnose
the combination rather than leave it as a property buried in the tool.

% ================================================================
% ################################################################
% ================================================================
\section{The Tool Landscape in Detail}
\label{sec:app-landscape}

\cref{tab:landscape-detailed} compares representative tools along the axes
on which their evaluators actually differ, expanding the summary in
\cref{sec:background}.

\begin{table}[ht]
\centering
\caption{How representative tools differ along the axes that shape their
evaluators.}
\label{tab:landscape-detailed}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lllllll}
\hline
Tool & Interface & Signal model & Online mode & Primary target & Auto-grad & Stack \\
\hline
Breach~\cite{donze2010breach,donze2013efficient} & API + DSL & piecewise-linear   & offline & falsification           & no  & MATLAB, mature \\
RTAMT~\cite{nickovic2020rtamt,nickovic2024rtamt} & DSL + API & discrete (default) / dense & delayed & runtime verification    & no  & Python, heavy \\
MoonLight~\cite{bartocci2023moonlight}           & DSL + API & spatial + temporal & partial & spatial RV              & no  & Java / CLI / Python \\
STLCG++~\cite{kapoor2025stlcgpp}                 & API       & discrete (tensor)  & none    & differentiable learning & yes & PyTorch \\
TaLiRo~\cite{annpureddy2011staliro}              & API + DSL & sampled (real-time) & offline & falsification          & no  & MATLAB \\
py-MTL~\cite{pyMTL}                              & API       & dense (hold)       & none    & monitoring              & no  & Python, light \\
% \tool     & DSL + API & piecewise-linear   & offline & evaluation              & no  & Python, light \\
\hline
\end{tabular}%
}
\end{table}

% ================================================================
\section{Experimental Details}
\label{sec:app-eval}
% ================================================================

This appendix records the protocols and full data behind the findings
reported in \cref{sec:evaluation}.

\paragraph{Artifact and reproducibility.}
The public \href{https://github.com/midoriao/tidystl}{artifact repository}
contains the implementation, fixtures, and reference-tool adapters. The
\href{https://github.com/midoriao/tidystl/blob/main/docs/usage.md}{user manual}
documents installation, backend semantics, and worked examples; the
\href{https://github.com/midoriao/tidystl/blob/main/extra/experiments/README.md}{experiment guide}
documents how to regenerate every evaluation record, table, and figure.

\paragraph{Diagnostic case set and protocol.}
The profile of \cref{tab:divergence-profile} uses one diagnostic case per
implicit choice, each a small formula and trace constructed so that only that
choice can affect the reported robustness. Every case runs on each tool in its
native environment---Breach (from a committed MATLAB cache), RTAMT in its
discrete and dense modes, STLCG++, py-MTL, and TaLiRo
(\texttt{dp\_taliro})---and we read the tool's per-timestep robustness at the
marked readout sample and map it to the option that output reveals. The
per-option robustness signature is predicted analytically and printed beside
the observed value, so each attribution is auditable rather than asserted. Two
rows additionally use a \tool reference to separate independent axes (the
signal-model row to tell dense from discrete time, the interpolation row to
tell piecewise-linear from piecewise-constant reconstruction). STLCG++ runs
with exact $\min$/$\max$ aggregation---its default; smooth aggregation is
opt-in---so its entries reflect its discrete-time semantics rather than
smoothing. TaLiRo's \texttt{dp\_taliro} reports only a whole-trace scalar at
$t=0$, so a choice that acts only on a later sample is marked \emph{n/o} (not
observable) rather than guessed.

\paragraph{Compatibility validation.}
Each \tool compatibility backend is validated separately against its tool's
committed reference fixtures---real-tool robustness regenerated from Breach,
RTAMT, STLCG++, py-MTL, and TaLiRo---by the artifact's per-tool reproduction
test suite, which checks per-timestep agreement at absolute tolerance
$10^{-6}$ on the operator-coverage regression suite. The backends implement
each tool's \emph{documented} rules; the check verifies the implementations
rather than defining them, and it is nontrivial: on the first live Breach run
it exposed two fidelity bugs in our own Breach-compatible backend (the
equality convention and the window-endpoint reduction), both fixed and
re-verified before the reported numbers were produced. One deviation is
deliberate: where real RTAMT silently reinterprets a non-uniform grid as
uniformly indexed samples, the RTAMT-compatible backend rejects the input
instead of reproducing the reinterpretation.

\paragraph{Localization procedure.}
Across any pair of validated backends, the localizer exploits that \tool
backends share one formula representation with inspectable per-node outputs:
it walks the shared syntax tree in post-order and reports the \emph{minimal
divergent node}---the lowest node whose outputs disagree while all its children
agree---together with each backend's primitive operation at that node. To
illustrate the output format, on two minimal single-operator cases (the
\emph{signal beyond horizon} and \emph{robustness at horizon} choices) the
listing reads:
\begin{lstlisting}[language={},keywordstyle={},basicstyle=\footnotesize\ttfamily]
first divergent op at `F[2,4]`: WindowMax[2,4] vs
    DiscreteWindowMax[2,4] (t-index 1) -> boundary rule
first divergent op at `and`: PointwiseMin vs PointwiseMin
    + terminal-step extension (t-index 2) -> terminal-step rule
\end{lstlisting}
The headline results are the tool-vs-tool localizations of
\cref{sec:discrepancies} (\cref{tab:e2-localization,fig:e2-window-sampling}),
where the same post-order walk pins a cause buried under several nesting levels.
Backend pairs whose lowerings differ structurally are compared at the
shared syntax-tree level rather than between lowered representations;
predicate-internal arithmetic is treated as a leaf, since none of the
implicit choices studied here concerns its internal operations.

\paragraph{Trace-family construction.}
A single constructed trace could sit on a knife edge, so each headline
case is widened to a swept family of 201 traces whose final sample varies
over $[-5,5]$, spanning regions of agreement and disagreement; no rule is
taken as the reference, and the rates are symmetric between the rules by
construction. For the boundary-rule family ($\Diamond_{[2,4]}(x \ge 0)$,
window past the trace end), the clamping and pessimistic rules return
opposite verdicts at the divergence timestep on exactly those traces whose
final sample is non-negative (101 of 201). The terminal-step family is a
conjunction violated only at the final sample; the as-is and
extend-penultimate rules flip the final verdict on 100 of 201 traces and
rank the trace oppositely against a fixed reference trace with an interior
violation---the ranking signal a falsification search acts on---on 80 of
201. These rates measure the width of the disagreement region within
each controlled family---the divergences are not measure-zero
coincidences---not a frequency claim about any particular workload.

\paragraph{Variant accounting.}
LOC in \cref{tab:variation} counts variant-specific code only: blank,
comment-only, and docstring lines are excluded, shared infrastructure is
excluded, and Rust is counted separately. The native backend is the
baseline and has no row; the Rust SIMD executor ships as an optional
native extension alongside the five backends. The full regression suite
comprises 181 tests and passes with all variants installed.

\paragraph{Performance sanity check.}
The benchmark in \cref{fig:scaling} compares \tool backends with real RTAMT and STLCG++ on
the same workload. On single traces, the explicit evaluator layer remains
within the same order of magnitude as the Python-stack references. On
batches, \tool's batch-first signal layout amortizes per-trace cost, making
the native backend substantially faster than looping RTAMT and faster than
the tensor-batched STLCG++ run shown here. The SIMD executor gives the
expected speed ceiling once per-node observability is dropped. This check
shows that inspectability does not impose prohibitive overhead; performance
is not a primary evaluation claim.

\begin{figure}[ht]
\centering
\input{figures/scaling-plot}
\caption{Mean evaluation time on representative workloads, plotted on a
log scale. All bars use the same formula
$\Box_{[0,5]}((x>0)\wedge\Diamond_{[0,2]}(y>0))$; error bars show one
standard deviation. RTAMT$^\dagger$ batch times are $N \times$ the
measured per-trace time; missing STLCG++ bars exceed memory (OOM).}
\label{fig:scaling}
\end{figure}

\Cref{tab:scaling} tabulates the measurements plotted in
\cref{fig:scaling}.

\begin{table}[ht]
\centering
\caption{Mean evaluation time (ms) $\pm$ std over a batch of $N$ traces of
length $T$; identical workload
$\Box_{[0,5]}((x>0)\wedge\Diamond_{[0,2]}(y>0))$ for every column. This is
the data plotted in \cref{fig:scaling}. $\dagger$: RTAMT evaluates one
trace per call, so batch times are $N \times$ the measured per-trace time
(linearity checked at $N \in \{1,8\}$). OOM: window materialization
exceeds memory on the 32\,GB host.}
\label{tab:scaling}
\input{figures/scaling-table}
\end{table}


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Comparison with Other Tools}\label[appendix]{section:detailed_comparison}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%-----------------------------------------------------------
\begin{table}[tp]
 \centering
 \caption{Comparison of tools supporting monitoring of robustness of STL or related logics. In the ``Supported Nominal Semantics'' column, ``classical'' refers to the robust semantics based on~\cite{donze2010robust} or~\cite{fainekos2009robustness} and ``RoSI-style'' refers to semantics based on the RoSI semantics~\cite{deshmukh2017online}.}
 \label{table:detailed_tool_comparison}
 \footnotesize
 \begin{tabular}{ccc}
  \toprule
  \multirow{2}{*}{Name} & \multirow{2}{*}{Supported Logic} & Supported \\
          &       & Nominal Semantics    \\\midrule
  Breach~\cite{donze2010breach}  & Future-time STL & classical \\
  TaLiRo~\cite{annpureddy2011staliro}  & Future-time STL & classical \\
  RTAMT~\cite{nickovic2024rtamt} & Future- and past-time STL & classical \\
  STLCG++~\cite{kapoor2025stlcgpp} & Future-time STL & classical and smooth~\cite{levenberg2020stlcg,pant2017smooth} \\
  SignalTemporalLogic.jl\footnote{https://github.com/sisl/SignalTemporalLogic.jl} & Future-time STL & classical and smooth~\cite{levenberg2020stlcg,pant2017smooth} \\
  py-signal-temporal-logic~\cite{pyMTL} & Future-time STL & classical \\
  mstlo~\cite{thomsen2026mstlo} & Future-time STL & classical and RoSI-style \\
  Argus\footnote{\url{https://github.com/anand-bala/argus}}   & Future-time STL & classical \\
  STLRom~\footnote{\url{https://github.com/decyphir/STLRom}}  & Future-time STL & RoSI-style \\
  % Reelay~\cite{reelay}  & Past-time STL & Boolean \\
  CauMon~\cite{CauMon}  & Future-time STL & causation semantics~\cite{DBLP:conf/cav/ZhangAAH23} \\
  MoonLight~\cite{bartocci2023moonlight} & STREL~\cite{DBLP:conf/memocode/BartocciBLN17} & classical \\
  \multirow{2}{*}{TempLogic\footnote{\url{https://github.com/fran-penedo/templogic}}}  & Future-time STL, & \multirow{2}{*}{classical} \\
  & TSSL~\cite{DBLP:journals/tcns/BartocciGHB18}, SpaTeL~\cite{DBLP:conf/hybrid/HaghighiJKBGB15} & \\\bottomrule
 \end{tabular}
\end{table}
%-----------------------------------------------------------

\cref{table:detailed_tool_comparison} presents several tools that support monitoring the robustness of STL or related logics, along with the nominal semantics they support.
The ``Supported Nominal Semantics'' column indicates the classification of the semantics supported by each tool.
The ``classical'' semantics refers to the robust semantics defined by inductively aggregating the robustness of atomic predicates using $\min$ and $\max$, as in~\cite{donze2010robust} and~\cite{fainekos2009robustness}.
``RoSI-style'' semantics refers to semantics based on robust satisfaction intervals (RoSI)~\cite{deshmukh2017online}, which indicate the range of robustness values that can be realized by extending the current trace with some suffix.

Currently, \tool{} supports the classical and smooth semantics as the nominal semantics.
Implementing compatibility backends of \tool{} for more tools supporting the classical semantics is a future work, which we expect to be straightforward, as we demonstrated in \cref{sec:implement-variations}.
Extending \tool{} to support the RoSI-style semantics~\cite{deshmukh2017online} and the causation semantics~\cite{DBLP:conf/cav/ZhangAAH23} is also a future work.
We believe that this can also be done by implementing appropriately compatibility backends.
